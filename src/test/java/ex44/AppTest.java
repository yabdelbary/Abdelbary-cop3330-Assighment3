package ex44;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;
import org.junit.Assert;


/*

class AppTest {

    @Test
    public void testSearch()
    {
        Object array;   //Three items below are given in the json file
        assertEquals("Widget", App.ProductDetails("Widget", array));
        assertEquals("Thing", App.ProductDetails"Thing", array));
        assertEquals("Doodad", App.ProductDetails("Doodad", array));

    }
}

 */
